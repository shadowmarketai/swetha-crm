# ============================================
# VoiceFlow Marketing AI - Production Dependencies
# Lightweight build for VPS (no GPU/ML libs)
# Uses API-based AI services + edge-tts fallback
# ============================================

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
asyncpg==0.29.0
psycopg2-binary>=2.9.0
pgvector>=0.3.0
redis>=4.5.2,<5.0.0

# Authentication & Security
passlib[bcrypt]==1.7.4
pyjwt==2.8.0
email-validator>=2.1.0
slowapi>=0.1.9
cryptography>=41.0.0

# AI/LLM Providers (API-based, lightweight)
anthropic>=0.7.0
openai>=1.3.0
groq>=0.3.0

# TTS (lightweight fallback - no GPU needed)
edge-tts>=6.1.0

# HTTP clients
httpx==0.25.2
aiohttp==3.9.1

# Background Tasks
celery[redis]==5.3.4

# Monitoring
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.35.0

# Payments (India)
razorpay>=1.4.0

# Utilities
python-dotenv==1.0.0
jinja2==3.1.2
python-dateutil==2.8.2
tenacity==8.2.3
aiofiles>=23.2.0
boto3==1.33.6

# Audio processing (lightweight, no torch)
pydub==0.25.1
soundfile==0.12.1
scipy==1.11.3

# PDF generation (PEB quotations)
reportlab>=4.0
