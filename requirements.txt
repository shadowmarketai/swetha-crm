# ============================================
# VoiceFlow Marketing AI - Dependencies
# BharatVoice + ZenVoice + Marketing Automation
# ============================================

# Core ML & Voice AI
torch==2.1.0
torchaudio==2.1.0
transformers==4.35.0
openai-whisper==20231117
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
scipy==1.11.3

# Indian Languages Support
ai4bharat-transliteration==1.0.0
indic-nlp-library==0.91

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart>=0.0.12  # CVE-2024-24762 ReDoS — fixed in 0.0.7+
websockets==12.0
pydantic==2.5.0
pydantic-settings==2.1.0
# anyio is pinned to <5 because FastAPI 0.104.1 declares anyio<4 in its
# install_requires, but google-genai pulls in anyio>=4. anyio 4 works fine in
# practice with FastAPI 0.104; the constraint here just suppresses the pip
# resolver warning and keeps a fresh `pip install -r requirements.txt`
# deterministic.
anyio>=4.0,<5

# Database
sqlalchemy==2.0.23
alembic==1.12.1
asyncpg==0.29.0
redis==5.0.1

# Authentication & Security
# NOTE: python-jose removed (KB-004: use PyJWT only)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.8.0
email-validator>=2.1.0
slowapi>=0.1.9

# CRM Integrations
zoho-crm-sdk==5.0.0
hubspot-api-client==8.1.0

# Marketing Integrations
facebook-business==18.0.4
google-ads==22.1.0
google-api-python-client==2.108.0
# Gemini 2.5 Flash Image (Nano Banana) for photoreal PEB renders
google-genai>=0.8.0

# Messaging Integrations
twilio==8.10.0

# Workflow Automation
httpx==0.25.2
aiohttp==3.9.1

# Analytics & Monitoring
prometheus-client==0.19.0
opentelemetry-api==1.21.0
sentry-sdk[fastapi]==1.35.0

# Utilities
python-dotenv==1.0.0
celery[redis]==5.3.4
boto3==1.33.6
jinja2==3.1.2
python-dateutil==2.8.2
tenacity==8.2.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

# Development
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1
ruff>=0.1.0

# AI/LLM Providers
anthropic>=0.7.0
openai>=1.3.0
groq>=0.3.0

# Voice Synthesis (TTS)
# elevenlabs>=0.2.0  # Uncomment when ready
deepgram-sdk>=2.11.0

# TTS Voice Cloning Module (VoiceFlow TTS)
edge-tts>=6.1.0          # Fallback TTS (CPU, no GPU needed)
sentencepiece>=0.1.99    # Indic Parler tokenization
accelerate>=0.25.0       # Model loading acceleration
# Optional heavy deps (uncomment for full GPU capability):
# TTS>=0.21.0            # XTTS-v2 (Coqui)
# git+https://github.com/ai4bharat/indic-parler-tts.git
# git+https://github.com/myshell-ai/OpenVoice.git
# git+https://github.com/myshell-ai/MeloTTS.git

# Payments (India)
razorpay>=1.4.0

# Additional Security
cryptography>=41.0.0

# Database - Production
psycopg2-binary>=2.9.0
pgvector>=0.3.0

# Background Tasks
flower>=2.0.0
apscheduler>=3.10

# Async File Handling
aiofiles>=23.2.0
